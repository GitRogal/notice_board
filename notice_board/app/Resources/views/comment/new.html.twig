{% extends 'base.html.twig' %}

{% block body %}
    <form action="{{ path("saveComment", {'id' : notice.id}) }}" method="post" role="form">
        <div class="row">
            <div class="col-sm-12">
                <textarea name="description" id="comments" style="font-family:sans-serif;font-size:1.2em;"></textarea>
            </div>
        </div>
        <input type="submit" value="Dodaj komentarz">
    </form>
{% endblock %}
